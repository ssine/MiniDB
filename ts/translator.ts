/* Generate relational-algebra expression (logical plan) from parse tree.
 * Input: a parse tree
 * Output: a logical plan
 */

class RATree {
  type: ('')
}

export function translate(tree: any): RATree {
  return new RATree();
}
